
import 'package:flutter/material.dart';

class FormS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Formulario de Sonido"),
    );
  }
}